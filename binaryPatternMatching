def binaryPatternMatching(pattern, s)
    vowels = ['a', 'e', 'i', 'o', 'u', 'y']
    s_string = s.split('')
    pattern_string = pattern.split('')
    i = 0
    loop do
        pattern_string.each do |num|
            if 
        i += 1
        break if i >= s_string.length
    end

end

s = 'amazing'
pattern = '010'

binaryPatternMatching(pattern, s)